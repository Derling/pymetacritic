from metacritic import PyGameCritic as pgn
import logging 
import matplotlib.pyplot as plt

#Module for generating bar graphs comparing the metacritic scores of a game
#accross multiple consoles. 
class MetaGraphs(plt):
    def __init(self):
        #game title as param,
        #try to get the docs for the consoles that the game was 
        # released on, if not in a certain console, pass(do nothing)
        pass
    